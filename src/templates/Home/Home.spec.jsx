/*geralmente se coloca a descrição do componente (no caso de teste a componentes) a ser testado e dentro do describe são colocados os
 testes deste componente*/
describe('<Home />', () => {
  it('is a dummy test', () => {
    expect(1).toBe(1);
  });
});
